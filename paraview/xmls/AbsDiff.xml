<ServerManagerConfiguration>
  <!-- This is the server manager configuration XML. It defines the interface to
       our new filter. As a rule of thumb, try to locate the configuration for
       a filter already in ParaView (in Servers/ServerManager/Resources/*.xml)
       that matches your filter and then model your xml on it -->
  <ProxyGroup name="filters">
   <SourceProxy
     name="ttkAbsDiff"
     class="ttkAbsDiff"
     label="TTK AbsDiff">

     <Documentation
        long_help="TTK absDiff plugin."
        short_help="TTK absDiff plugin.">
          TTK absDiff plugin documentation.
     </Documentation>

      <InputProperty
          name="Input"
          command="AddInputConnection"
          clean_command="RemoveAllInputs"
          multiple_input="1">
        <ProxyGroupDomain name="groups">
          <Group name="sources"/>
          <Group name="filters"/>
        </ProxyGroupDomain>
        <DataTypeDomain name="input_type">
          <DataType value="vtkDataSet"/>
        </DataTypeDomain>
        <Documentation>
          Data-set to process.
        </Documentation>
      </InputProperty>

       <IntVectorProperty
           name="Function"
           label="Function"
           command="SetFunction"
           number_of_elements="1"
           default_values="0"
           >
         <EnumerationDomain name="enum">
          <Entry value="0" text="Absolute Difference"/>
          <Entry value="1" text="Ratio"/>
          <Entry value="2" text="Relative Difference"/>
        </EnumerationDomain>
        <Documentation>
            Backend for the computation of the persistence diagram.
            The progressive and approximate approaches only allow the computation of saddle-extremum pairs on regular grids.
        </Documentation>
      </IntVectorProperty>

      <PropertyGroup panel_widget="Line" label="Output options">
        <Property name="Function" />
      </PropertyGroup>

     ${DEBUG_WIDGETS}

      <Hints>
        <ShowInMenu category="TTK - Ensemble Scalar Data" />
      </Hints>
   </SourceProxy>
 </ProxyGroup>
</ServerManagerConfiguration>
